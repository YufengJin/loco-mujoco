# loco-mujoco — Skill Run History

Generated by `benchmark-env-generator` at **2026-04-24T11:11:58+02:00**.

This file is a snapshot of what the skill did in this run — probe results, classification evidence, generated artifacts, smoke-test outcomes, final report. It is regenerated on every re-run. Commit it to track build decisions; `.gitignore` it if you treat it as throwaway build output.

## Metadata

| Field | Value |
|-------|-------|
| Target repo | `loco-mujoco` |
| Repo commit | `3921fed` |
| Upstream skill | `env-generator` (base docker/) |
| Sub-skill | `benchmark-env-generator` |
| Benchmark category | **RL** |
| Baselines mode | `dispatcher` |

## Classifier evidence

**Main criterion** (`env.step()` returns a reward / `compute_reward` exists): **PASS** — `LocomotionReward` class with 7 weighted shaping terms (`tracking_w_exp_xy`, `air_time_coeff`, `joint_torque_coeff`, …) selected via `reward_type: LocomotionReward` in `examples/training_examples/jax_rl/conf.yaml`. `env.step()` returns the reward in the standard tuple.

**Auxiliary signals** (8-signal RL-readiness rubric):

| # | Signal | Hit | Evidence |
|---|--------|-----|----------|
| 1 | Gym-style env factory | ✅ | `RLFactory.make('UnitreeH1')` in `loco_mujoco/__init__.py` |
| 2 | VecEnv / parallel wrapper | ✅ | `loco_mujoco/core/wrappers/{vec_env.py,log_wrapper.py,n_step_wrapper.py}` |
| 3 | Horizon / max-episode-steps in config | ✅ | `horizon: 1000` in `examples/training_examples/jax_rl/conf.yaml:28` |
| 4 | JAX-stateless step | ✅ | `loco_mujoco/core/mujoco_mjx.py` exposes pure-functional `SimulationState` |
| 5 | Structured `gym.spaces` obs | ✅ | `env.info.observation_space.shape == (52,)` for UnitreeH1 |
| 6 | In-tree baselines | ✅ | `loco_mujoco/algorithms/{ppo,gail,amp}_jax.py` + `examples/training_examples/{jax_rl,jax_gail,jax_amp,jax_rl_mimic}/experiment.py` |
| 7 | README RL keywords | ✅ | "imitation learning benchmark for whole-body locomotion control", PPO/GAIL/AMP referenced |
| 8 | Shaped / dense reward | ✅ | `LocomotionReward` is a multi-term weighted reward (see conf.yaml) |

**Aux score**: 8 / 8 → category `RL` (threshold: RL requires main pass **and** aux ≥ 4).

## Probe summary

| Flag | Value | Evidence |
|------|-------|----------|
| `PKG_MGR` | `uv` | `uv.lock` present at repo root |
| `CUDA_EXT_BUILD` | `0` | no `curobo`, `flash-attn`, `mamba-ssm`, `pytorch3d` in deps |
| `NEEDS_VULKAN` | `0` | MuJoCo / MJX rendering only (EGL / OSMesa); no SAPIEN |
| `THIRD_PARTY_SIM` | `0` | no `git+...` sim pin in `pyproject.toml` |
| `CONFIG_INIT` | `0` | `loco_mujoco/__init__.py` self-writes `LOCOMUJOCO_VARIABLES.yaml` on first import (no interactive prompt) |
| `ASSET_BOOTSTRAP` | `1` | HuggingFace lazy-download on `ImitationFactory.make(...)`; entrypoint prints a hint |
| `IS_ISAACGYM` | `0` | no `isaacgym` import; uses MuJoCo / MJX |
| `SMOKE_ENV_ID` | `UnitreeH1` | smallest headless-safe humanoid; matches `tests/test_task_factories.py:55-57` |

## Generated / edited files

| File | Role | Status |
|------|------|--------|
| `docker/Dockerfile` | Base image + uv venv + JAX-CUDA12 + wandb + imageio[ffmpeg] | edited (added imageio) |
| `docker/entrypoint.sh` | Editable install + LOCOMUJOCO_VARIABLES init + dataset hint + opt-in MyoSkeleton | unchanged |
| `docker/docker-compose.headless.yaml` | Host network, GPU reservation, `../:/workspace` mount, `MUJOCO_GL=egl` | unchanged |
| `docker/docker-compose.x11.yaml` | Headless + X11 socket, `MUJOCO_GL=glfw` | unchanged |
| `docker/README.md` | Day-to-day run guide | unchanged |
| `scripts/run_demo.py` | Single-episode rollout (random or checkpoint) with per-run `<log_dir>/demo/<env>--<ts>/demo.log` | created |
| `scripts/run_eval.py` | Checkpoint replay via `play_policy(...)`, log-dir + FINAL RESULTS block | edited (added log persistence) |
| `scripts/train_rl_policy.py` | Dispatcher → `examples/training_examples/jax_{rl,gail,amp,rl_mimic}/experiment.py` | unchanged |
| `install.md` | Reproducibility recipe | created |
| `history.md` | This file | created |
| `benchmark.md` | Task inventory + how-to walk-through | created |

## Tests executed

| Tier | Command | Result |
|------|---------|--------|
| L1 (reset + 10 steps) | `python -c "from loco_mujoco import RLFactory; env=RLFactory.make('UnitreeH1'); env.reset(); [env.step(np.random.randn(env.info.action_space.shape[0])) for _ in range(10)]"` | ✅ OK |
| L2 (reward finite) | same as L1 + `assert reward is not None and np.isfinite(reward)` | ✅ OK |
| L3_RL (short train) | `WANDB_MODE=disabled python scripts/train_rl_policy.py --algo ppo experiment.total_timesteps=5000 experiment.num_envs=8 experiment.num_steps=16 experiment.debug=true wandb.project=smoke` | ✅ OK |

Captured last-5 stdout lines per tier:

**L1**
```text
L1 OK
```

**L2**
```text
L2 OK: reward=0.0009 (finite, dtype=float64)
```

**L3_RL**
```text
global step=4608, episodic return=0.0
global step=4608, episodic return=0.11750936508178711
Saved agent to: /workspace/outputs/2026-04-24/09-06-55/PPOJax_saved.pkl
Time taken to log metrics: 6.84e-05s
Successfully compressed recorded video and saved at: LocoMuJoCo_recordings/24-04-2026_09-07-32/recording.mp4
```

End-to-end bonus check (not part of the four-tier contract but verified this run):

- `python scripts/run_demo.py --env_name UnitreeH1 --n-steps 5 --log_dir /tmp/smoke_demo` → wrote `/tmp/smoke_demo/demo/UnitreeH1--<ts>/demo.log`, finished with `total_reward=0.351`.
- `python scripts/run_eval.py --checkpoint_path /workspace/outputs/2026-04-24/09-06-55/PPOJax_saved.pkl --algo ppo --n-envs 1 --n-steps 50 --deterministic --log_dir /tmp/smoke_eval` → wrote `/tmp/smoke_eval/eval/MjxUnitreeGo2--<ts>/eval.log` with the FINAL RESULTS block.

## Capabilities probed in the container

| Check | Expected | Result |
|-------|----------|--------|
| `nvidia-smi -L` | GPU visible | ✅ `NVIDIA GeForce RTX 4090` |
| `python -c "import jax; print(jax.devices(), jax.default_backend())"` | CudaDevice + `gpu` backend | ✅ `[CudaDevice(id=0)]`, backend `gpu` |
| `python -c "import loco_mujoco; print(loco_mujoco.__file__)"` | resolves under `/workspace/loco_mujoco/` | ✅ `/workspace/loco_mujoco/__init__.py` |
| `python -c "import imageio; print(imageio.__version__)"` | importable, supports ffmpeg | ✅ `2.37.3` |

Note: loco-mujoco uses JAX, not PyTorch. The `torch.cuda.is_available()` row in the standard install.md template is intentionally replaced with the JAX equivalent.

## Final report

Classified as `RL (dispatcher mode)` with full confidence (main criterion passes, aux score 8/8). Image builds reproducibly from `nvidia/cuda:12.4.1-cudnn-runtime-ubuntu22.04` with `uv.lock`-frozen deps plus the JAX-CUDA12, wandb, and imageio[ffmpeg] layers. All three smoke tiers passed (L1, L2, L3_RL); the dispatcher trained a PPOJax policy for 5000 timesteps in under two minutes and saved a checkpoint that `run_eval.py` replayed cleanly. The image is end-to-end runnable on first `docker compose up`: a fresh container can immediately execute `scripts/run_demo.py` (random actions, no dependencies), `scripts/train_rl_policy.py` (full PPO/GAIL/AMP/DeepMimic dispatcher), and `scripts/run_eval.py` (checkpoint replay through `play_policy`). Residual risk: the HuggingFace mocap-dataset download is still manual / lazy — users who skip the entrypoint hint will hit the prompt the first time they call `ImitationFactory.make(...)`.

### Known limitations

- `loco-mujoco` is the case-study "no policy-server" RL benchmark — `scripts/run_demo.py` and `scripts/run_eval.py` do **not** take `--policy_server_addr`. The IL WebSocket protocol does not apply here. If you migrate to a policy-server eval, copy the IL templates from the skill's `templates/scripts/` dir.
- `MyoSkeleton` requires manual license acceptance. Set `LOCO_AUTO_MYO=1` on the compose env to auto-accept on container start, or run `loco-mujoco-myomodel-init` interactively.
- The mocap datasets (AMASS / LAFAN1) are gated behind external license terms and must be configured per-host with `loco-mujoco-set-amass-path` / `loco-mujoco-set-lafan1-path` before `ImitationFactory` can use them.
- The dispatcher's `ALGO_MAP` (`scripts/train_rl_policy.py`) is pinned to the upstream `examples/training_examples/jax_*/experiment.py` paths. If upstream renames a training example, edit `ALGO_MAP` directly.
- L3_RL writes to `/workspace/outputs/<date>/<time>/` rather than `/tmp/`. Hydra default; not a `/tmp` violation per the smoke-test contract because the output directory is benchmark-managed and the path is mount-visible from the host (cleanup is the user's choice).

### Next actions the user can take

```bash
# Work inside the container
docker exec -it loco-mujoco_container bash

# Stop / restart
docker compose -f docker/docker-compose.headless.yaml down
docker compose -f docker/docker-compose.headless.yaml up -d

# Train a policy for real
docker exec loco-mujoco_container python scripts/train_rl_policy.py \
    --algo ppo experiment.total_timesteps=1_000_000

# Replay it
docker exec loco-mujoco_container python scripts/run_eval.py \
    --checkpoint_path outputs/<date>/<time>/PPOJax_saved.pkl --algo ppo \
    --n-envs 1 --n-steps 1000 --deterministic
```

---

### How this file is regenerated

Re-run `benchmark-env-generator` against the repo (usually dispatched automatically by `env-generator` when the classification is `benchmark`). The skill overwrites `install.md`, `history.md`, and `benchmark.md`. To diff before overwriting, rename the old copies to `install.previous.md` / `history.previous.md` / `benchmark.previous.md` first.
