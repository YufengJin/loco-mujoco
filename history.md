# loco-mujoco — Skill Run History

Generated by `benchmark-generator` at **2026-05-03T07:56:00+02:00**.

This file is a snapshot of what the skill did in this run — probe results, classification evidence, generated artifacts, smoke-test outcomes, final report. It is regenerated on every re-run. Commit it to track build decisions; `.gitignore` it if you treat it as throwaway build output.

## Metadata

| Field | Value |
|-------|-------|
| Target repo | `loco-mujoco` |
| Repo commit | `b5face8` (branch `master`) |
| Upstream skill | `env-generator` (base docker/) — image `yufengjin/loco-mujoco:latest` (id `4bc361239ead`) |
| Sub-skill | `benchmark-generator` |
| Benchmark category | **RL** |
| Baselines mode | `dispatcher` |

## Classifier evidence

**Main criterion** (`env.step()` returns a reward / `compute_reward` exists): **PASS** — `env.step()` in `loco_mujoco/core/mujoco_base.py` returns `(obs, reward, absorbing, done, info)` (5-tuple). Reward is shaped via `LocomotionReward` (RL) or `MimicReward`/`AMPReward` (imitation), all in `loco_mujoco/core/reward/`. README intentionally markets the project as an "imitation-learning benchmark", but the actual env interface is RL — imitation is just a `task_factory` flavour layered on top of the same gym-style step contract.

**Auxiliary signals** (8-signal RL-readiness rubric):

| # | Signal | Hit | Evidence |
|---|--------|-----|----------|
| 1 | Gym-style env factory | yes | `RLFactory.make('UnitreeH1')` exported from `loco_mujoco/__init__.py` |
| 2 | VecEnv / parallel wrapper | yes | `loco_mujoco/core/wrappers/{vec_env.py,log_wrapper.py,n_step_wrapper.py}` |
| 3 | Horizon / max-episode-steps in config | yes | `horizon: 1000` in `examples/training_examples/jax_rl/conf.yaml` |
| 4 | JAX-stateless step | yes | `loco_mujoco/core/mujoco_mjx.py` exposes pure-functional `SimulationState` |
| 5 | Structured `gym.spaces` obs | yes | `env.info.action_space.shape == (19,)` for UnitreeH1 |
| 6 | In-tree baselines | yes | `loco_mujoco/algorithms/{ppo_jax,gail_jax,amp_jax}.py` + 4 `examples/training_examples/jax_*/experiment.py` |
| 7 | README RL keywords | yes | "PPO, GAIL, AMP, DeepMimic" called out as first-class baselines |
| 8 | Shaped / dense reward | yes | `LocomotionReward` is a 7-term weighted reward (XY/yaw tracking, air time, joint torque, action rate, energy, joint limits, symmetry) |

**Aux score**: 8 / 8 → category `RL` (threshold: RL requires main pass and aux >= 4).

**Sub-mode decision**: `dispatcher` — repo ships in-tree algorithm classes (`PPOJax`, `GAILJax`, `AMPJax`) + 4 hydra-driven `experiment.py` entrypoints under `examples/training_examples/`. The dispatcher script (`scripts/train_rl_policy.py`) routes `--algo ppo|gail|amp|rl_mimic` to the matching upstream `experiment.py`. **No sb3 train script** is generated — sb3 is not installed and not needed for this benchmark.

## Probe summary

| Flag | Value | Evidence |
|------|-------|----------|
| `PKG_MGR` | `uv` | `uv.lock` (743 KB) at repo root |
| `CUDA_EXT_BUILD` | `0` | no curobo / flash-attn / mamba-ssm / pytorch3d in deps |
| `NEEDS_VULKAN` | `0` | MuJoCo / MJX rendering only (EGL); no SAPIEN |
| `THIRD_PARTY_SIM` | `0` | no `git+...` sim pin in `pyproject.toml` |
| `CONFIG_INIT` | `0` | `loco_mujoco/__init__.py` self-writes `LOCOMUJOCO_VARIABLES.yaml` on first import |
| `ASSET_BOOTSTRAP` | `1` | HuggingFace lazy-download on `ImitationFactory.make(...)` |
| `IS_ISAACGYM` | `0` | no `isaacgym` import; uses MuJoCo / MJX |
| `SMOKE_ENV_ID` | `UnitreeH1` | smallest headless-safe humanoid env (19-D action space) |

## Generated / edited files

| File | Role | Status |
|------|------|--------|
| `docker/Dockerfile` | env-generator output: nvidia/cuda:12.8.1 + uv venv + JAX-CUDA12 + wandb + imageio[ffmpeg] | not touched (no extra pip needed for dispatcher mode) |
| `docker/entrypoint.sh` | env-generator output: editable install + LOCOMUJOCO_VARIABLES init | not touched |
| `docker/docker-compose.headless.yaml` | env-generator output: GPU reservation, `../:/workspace` mount, `MUJOCO_GL=egl` | not touched |
| `docker/docker-compose.x11.yaml` | env-generator output: headless + X11 socket, `MUJOCO_GL=glfw` | not touched |
| `scripts/run_demo.py` | Single-episode rollout (random or checkpoint) with per-run `<log_dir>/demo/<env>--<ts>/demo.log` | reused (already conformant) |
| `scripts/run_eval.py` | Checkpoint replay via `play_policy(...)`, log-dir + FINAL RESULTS block | reused (already conformant) |
| `scripts/train_rl_policy.py` | Dispatcher → `examples/training_examples/jax_{rl,gail,amp,rl_mimic}/experiment.py` | reused (already conformant) |
| `install.md` | env-generator output | not touched (owned by env-generator) |
| `history.md` | This file | regenerated |
| `benchmark.md` | Task inventory + how-to walk-through | regenerated |

`diagnostics_applied`: empty — the run was clean, no smoke failures required env edits.

## Tests executed

| Tier | Command | Result |
|------|---------|--------|
| L1 (container reachability + import) | `docker compose exec loco-mujoco python -c "import loco_mujoco; print(loco_mujoco.__version__)"` | PASS — `1.1.0` |
| L2 (env build + 10-step rollout + reward sanity) | `RLFactory.make('UnitreeH1')` + `env.reset()` + 10× `env.step(env.sample_action_space(key))` + `assert np.isfinite(reward)` | PASS — `action_dim=19, last_reward=0.1333, total_reward_10=0.2754` |
| L3_RL (random-action `run_demo.py`) | `python scripts/run_demo.py --env_name UnitreeH1 --n-steps 20` | PASS — `total_reward=1.429, runtime=4.03s` |

Captured last-5 stdout lines per tier:

**L1**
```text
L1 import OK: 1.1.0
```

**L2**
```text
L2 OK: action_dim=19, last_reward=0.1333, total_reward_10=0.2754
```

**L3_RL** (`scripts/run_demo.py --env_name UnitreeH1 --n-steps 20`)
```text
step 17: reward=0.01704680025686102 done=False
step 18: reward=0.032536653250830744 done=False
step 19: reward=0.024440825084822402 done=False

demo finished: 20 steps, total_reward=1.429
log_file: ./logs/demo/UnitreeH1--20260503_075619/demo.log
```

Note on L2 implementation: loco-mujoco's `env.reset()` returns the obs ndarray directly (not a `(obs, info)` 2-tuple), and `env.sample_action_space(key)` requires a JAX PRNG key. The L1/L2 check uses `env.info.action_space.shape[0]` (action_dim is exposed via `MDPInfo`, not as a top-level `env.action_space` attribute). This is a loco-mujoco-specific API pattern, not a regression.

## Capabilities probed in the container

| Check | Expected | Result |
|-------|----------|--------|
| `python -c "import loco_mujoco; print(loco_mujoco.__version__)"` | importable | PASS — `1.1.0` |
| `python -c "import jax; print(jax.devices())"` | CudaDevice + `gpu` backend | PASS (env-generator tier1 partial recorded earlier; JAX 0.6.2 sees `[CudaDevice(id=0)]`) |
| `python -c "import torch"` | n/a — torch NOT installed by design | **EXPECTED FAIL** — `ModuleNotFoundError: No module named 'torch'`. loco-mujoco is JAX-only; the env-generator tier1 `torch_cuda=fail` row is a known false-positive for this repo. Do NOT count it as a regression. |
| `RLFactory.make('UnitreeH1').step(...)` | 5-tuple `(obs, reward, absorbing, done, info)`, finite reward | PASS — see L2 above |
| `scripts/run_demo.py --env_name UnitreeH1 --n-steps 20` | 20 random-action steps, log file written | PASS — see L3_RL above |

Note: loco-mujoco is JAX-only (PyTorch is not in the dependency closure). The standard `torch.cuda.is_available()` capability check is replaced with the JAX-equivalent `jax.devices()` row above. The benchmark-generator tier1 contract is therefore relaxed for this repo — torch_cuda=fail is the documented JAX-only exception.

## Final report

Classified as `RL (dispatcher mode)` with full confidence (main criterion passes; aux score 8/8). The image is end-to-end runnable on the env-generator output: container `loco-mujoco-headless` (image `yufengjin/loco-mujoco:latest`, id `4bc361239ead`) is up, editable install resolves, and all three smoke tiers passed.

L3_RL was exercised via the lightweight `scripts/run_demo.py --env_name UnitreeH1 --n-steps 20` (random actions, no checkpoint required, ~4s wall-clock) — the user-validated minimal smoke for this repo. The heavier dispatcher-train smoke (`scripts/train_rl_policy.py --algo ppo experiment.total_timesteps=5000`) is documented as the next-action recipe for users; it was exercised in the previous run (2026-05-02) and is known to terminate cleanly in <1 min.

No Dockerfile patch was needed: dispatcher mode delegates to upstream's own JAX trainers, which already pull `hydra`, `omegaconf`, `wandb`, and `flax` from `uv.lock`. `stable-baselines3` is intentionally NOT installed (loco-mujoco does not use it). All three scripts (`scripts/run_demo.py`, `scripts/run_eval.py`, `scripts/train_rl_policy.py`) from the prior run were already conformant (proper module-docstring `Purpose`/`Example` sections, `--log_dir`/`--save_video` arguments, signal-handled cleanup) and were reused without modification.

### Known limitations

- README markets loco-mujoco as an "imitation learning benchmark for whole-body locomotion control", but the env interface is RL (`env.step` returns shaped reward; `RLFactory` is the default factory). The classifier resolves the framing/interface mismatch by trusting the interface (8/8 aux score). Imitation is available as `ImitationFactory` and through the `gail`/`amp`/`rl_mimic` dispatcher algos — both still go through the same RL-style step contract.
- No WebSocket policy-server protocol — `scripts/run_demo.py` and `scripts/run_eval.py` do **not** take `--policy_server_addr`. This is the case-study "no policy-server" RL benchmark.
- The mocap datasets (AMASS / LAFAN1) are gated behind external license terms and must be configured per-host with `loco-mujoco-set-amass-path` / `loco-mujoco-set-lafan1-path` before `ImitationFactory` can use them.
- `MyoSkeleton` requires manual license acceptance — set `LOCO_AUTO_MYO=1` on the compose env to auto-accept on container start, or run `loco-mujoco-myomodel-init` interactively.
- The dispatcher's `ALGO_MAP` (`scripts/train_rl_policy.py`) is pinned to the upstream `examples/training_examples/jax_*/experiment.py` paths. If upstream renames a training example, edit `ALGO_MAP` directly.
- L3_RL writes to `/workspace/outputs/<date>/<time>/` rather than `/tmp/` (Hydra default); not a `/tmp` violation per the smoke-test contract because the path is benchmark-managed and mount-visible from the host.
- benchmark-spec.json is intentionally NOT generated (RL benchmarks; spec capture is IL-only per skill contract).
- PyTorch is intentionally absent from the image; do not treat `import torch` failures as regressions — they are by design.

### Next actions the user can take

```bash
# Work inside the container
docker compose -f docker/docker-compose.headless.yaml exec loco-mujoco bash

# Stop / restart
docker compose -f docker/docker-compose.headless.yaml down
docker compose -f docker/docker-compose.headless.yaml up -d

# Train a policy for real (dispatcher mode -> upstream PPOJax)
docker compose -f docker/docker-compose.headless.yaml exec loco-mujoco python scripts/train_rl_policy.py \
    --algo ppo experiment.total_timesteps=1_000_000

# Replay it
docker compose -f docker/docker-compose.headless.yaml exec loco-mujoco python scripts/run_eval.py \
    --checkpoint_path outputs/<date>/<time>/PPOJax_saved.pkl --algo ppo \
    --n-envs 1 --n-steps 1000 --deterministic
```

---

### How this file is regenerated

Re-run `benchmark-generator` against the repo (usually dispatched automatically by `env-generator` when the classification is `benchmark`). The skill overwrites `history.md` and `benchmark.md`; `install.md` is owned by `env-generator` and is not touched. To diff before overwriting, rename the old copies to `history.previous.md` / `benchmark.previous.md` first.
